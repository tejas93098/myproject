// O_NOFOLLOW (0x20000) is not supported by default
NODEFS.flagsForNodeMap[0x20000] = 0;

function sqliteError(code, msg, errno) {
  const error = new Error(code + ': ' + msg);
  error.errno = errno;
  error.code = code;

  return error;
}

module.exports.sqliteError = sqliteError;
